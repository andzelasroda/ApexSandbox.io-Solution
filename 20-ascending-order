public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    if((b>a)&&(c>b)||(b==a)&&(c>b)){
        return TRUE;
    }
    if((b==a)&&(b==c)){
        return TRUE;
    }
    else{
        return FALSE;
    }
}